import { Account } from './account';
import { Stock } from './stock';
import { Tax } from './tax';

export class ProductHistory {
  id: number;
  userid: string;
  action: string;
  notes: string;
}

